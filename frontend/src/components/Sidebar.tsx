export interface HistoryItem { id: string; title: string; }
interface Props { filter: string; analysis: string; history: HistoryItem[]; onFilter: (value: string) => void; onAnalysis: (value: string) => void; onDownload: () => void; onSelect: (id: string) => void; }
export default function Sidebar({ filter, analysis, history, onFilter, onAnalysis, onDownload, onSelect }: Props) { return <aside className="sidebar"><div className="brand"><span>J</span><div><b>Jira QA</b><small>Pipeline de análisis</small></div></div><label>Filtro de búsqueda<textarea value={filter} onChange={(e) => onFilter(e.target.value)} placeholder="Ej. Sprint activo, bloqueados..." /></label><label>Información a analizar<textarea value={analysis} onChange={(e) => onAnalysis(e.target.value)} placeholder="Ej. Riesgos, SLA, prioridades..." /></label><button className="download" onClick={onDownload}>↓ Descargar reporte Excel</button><div className="history"><p>CONSULTAS ANTERIORES</p>{history.length ? history.map((item) => <button key={item.id} onClick={() => onSelect(item.id)}>{item.title}</button>) : <small>Las consultas aparecerán aquí.</small>}</div></aside>; }
